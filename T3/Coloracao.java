public class Coloracao {
    
    public Coloracao(Grafo g) {
        
        int len = (int)Math.pow(2, g.getConjuntoVertices().size());

        double x[] = new double[len];
        x[0] = (double)0;
        int ss = len;
    }
}